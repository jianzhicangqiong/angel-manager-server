server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/angel?useUnicode=true&charaterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

#logging:
#  level:
#    com.hero.demo.mapper : debug
logging:
  level:
    com.hero.demo.*: DEBUG
    org.springframework:
      security: DEBUG
# 初始配置
init:
  init-role-id: 1
